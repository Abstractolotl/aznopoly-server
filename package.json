{
  "name": "aznopoly-server",
  "module": "src/index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "^1.1.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run --bun src/index.ts",
    "build": "bun build src/index.ts --outdir ./build",
    "test": "bun test"
  }
}